libraries: iomanip, iostream, cstdlib, ctime, vector

prior to running
  start random number generator
  initialize symbols (with vectors)
  3 visible tiles
  visible symbols w/ number of tiles using vector<string>
  starting balance (100)
  bet

display welcome message

while balance > 0
  display starting balance
  ask user for bet
  user inputs bet

  if bet > balance
    display error message: invalid input
    have user enter again (continue)

deduct bet

spin wheel:
  for int i = 0; i < # of tiles
    random index = random number
    tiles[i] = symbols[random index]

display reels
for (string &symbol : reels)
  symbol 1 | 2 | 3 separated by spaces

checking winning combinations
initialize wins = 0
three of a kind
  if tiles[0] == tile[1] && tile[1] == tile[2]
    if tile[0] == Orange
      multiply bet by 5
    else if tile[0] == Diamond
      multiply bet by 10
    else if tile[0] == Cherry
      multiply bet by 15
    else if tile[0] = Melon
      multiply bet by 20
    else if tile[0] = Bell
      multiply bet by 50
    else if tile[0] = Grape
      multiply bet by 100
    else if tile[0] = Seven
      multiply bet by 200
    display how much the user won
two of a kind
  else if(tile[0] == tile[1] || tile[1] == tile[2] || tile[0] == tile[2])
    multiply bet by 2
    display result
display losing message otherwise
add balance

display final balance

play again?
user input y or n
if y
    repeat
if n
    end program

return 0
